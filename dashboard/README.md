# InitiatorAPI Dashboard

Vite + React UI that renders `scanner/output/findings.json`.

```bash
cd dashboard
npm install
npm run sync-findings   # copy latest scanner output into public/
npm run dev
```

Open `http://localhost:5173`.
